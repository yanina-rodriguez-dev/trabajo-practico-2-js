// Realiza un script que pida un texto y lo muestre en mayúsculas.

let frase = prompt('Ingrese su frase');

console.log(frase.toLocaleUpperCase());

frase = frase.toLocaleUpperCase();

document.write(frase);

